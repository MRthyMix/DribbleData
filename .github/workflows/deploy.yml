name: Deploy to Pi

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - name: Pull latest code
        run: cd ~/DribbleData && git pull

      - name: Restart app
        run: sudo systemctl restart dribbledata
